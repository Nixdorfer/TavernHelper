Input

World book JSON folder node group nested hierarchy node value is group array

Setting collection each line starts 4 char serial A1b2-content upper lower digits lines not this format not setting

Add modify delete during progression
[new_node]({"desc":"node desc, shortest","add":{"node.group":"content"},"change":{"serial":"content"},"del":["serial"]})
path dot separator auto create if not exist omit unchanged skip if no important changes

Call as needed keep minimal

Revise only after withdraw revision response
[revision_node]({"serial":"content"})

Output memory/summary only in above format